import { TripPost } from "@/types/types";
import React from "react";

export interface Props {
    data?: TripPost,
    small?: boolean,
}

const Post: React.FC<Props> = ({data, small}) => {
    return <></>
}

export default Post;